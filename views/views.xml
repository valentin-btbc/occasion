<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="occasion.list">
      <field name="name">occasion list</field>
      <field name="model">occasion.occasion</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="occasion.action_window">
      <field name="name">occasion window</field>
      <field name="res_model">occasion.occasion</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="occasion.action_server">
      <field name="name">occasion server</field>
      <field name="model_id" ref="model_occasion_occasion"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="occasion" id="occasion.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="occasion.menu_1" parent="occasion.menu_root"/>
    <menuitem name="Menu 2" id="occasion.menu_2" parent="occasion.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="occasion.menu_1_list" parent="occasion.menu_1"
              action="occasion.action_window"/>
    <menuitem name="Server to list" id="occasion" parent="occasion.menu_2"
              action="occasion.action_server"/>
    -->
  </data>
</odoo>